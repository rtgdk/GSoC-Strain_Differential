import { Card } from './Card';
import { CardGroup } from './CardGroup';
export declare class HandRank {
    static HIGH_CARD: number;
    static PAIR: number;
    static TWO_PAIRS: number;
    static TRIPS: number;
    static STRAIGHT: number;
    static FLUSH: number;
    static FULL_HOUSE: number;
    static QUADS: number;
    static STRAIGHT_FLUSH: number;
    protected rank: number;
    protected highcards: Card[];
    protected constructor(rank: number, highcards: Card[]);
    getHighCards(): Card[];
    getRank(): number;
    compareTo(handrank: HandRank): number;
    static evaluate(cardgroup: CardGroup): HandRank;
    toString(): string;
}
